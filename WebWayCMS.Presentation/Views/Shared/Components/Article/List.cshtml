@model WebWayCMS.Models.Article.ArticleListViewModel
@using WebWayCMS.Models.Article

@{
	ViewData["Title"] = "Article";
	ViewData["ArticleListSlug"] = Model.ArticleListSlug;
}

<section class="section">
	<div class="container">
		<div class="columns is-multiline">
			@foreach (var article in Model.Articles)
			{
				@await Component.InvokeAsync("Article", new ArticleContentZoneConfiguration { ViewName = "Article", Article = article })
			}
		</div>
	</div>
</section>