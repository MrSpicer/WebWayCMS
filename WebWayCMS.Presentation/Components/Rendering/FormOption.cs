namespace WebWayCMS.Presentation.Rendering;

/// <summary>A selectable option (value + display label) for a picker-style form field.</summary>
public sealed record FormOption(string Value, string Label);
